#include <stdio.h>
#include <math.h>

int main(void) {
    // Example of tried keys and n:
    // e (Public Key) : 3001
    // d (Private Key) : 841
    // n (P * Q) : 421999
    // Encrypt ex. : H (72) -> 351180

    unsigned long long int exponent = 3001;
    unsigned long long int base = 72;
    unsigned int divisor = 421999;

    return 0;
}